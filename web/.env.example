# Only needed for a deploy where the frontend and API are separate Vercel
# projects (different domains). Local dev leaves this unset — Vite's dev
# server proxies /api to http://localhost:4000 (see vite.config.ts).
# VITE_API_BASE_URL="https://your-api-project.vercel.app"
